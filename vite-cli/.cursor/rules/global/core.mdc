---
description: 核心开发规范，包含代码质量检查要求和响应语言设置，适用于所有场景。
globs: 
alwaysApply: true
---

# 核心开发规范

## 代码质量检查
- **完成自检**：每次输出代码后，必须进行以下检查：
  1. 语法是否正确，能否正常编译/运行
  2. 逻辑是否完整，是否有遗漏的边界情况
  3. 是否符合本规则中的命名约定和代码风格
  4. 是否存在重复代码，是否可以复用现有逻辑
- **错误处理**：确保关键操作有适当的错误处理机制

## 响应语言
- 所有回复必须使用**简体中文**
